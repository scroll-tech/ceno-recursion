from "poseidon" import poseidon
from "poseidon_hash" import compress

struct Transcript {
    field[12] state
}

def new_transcript(field[0] label) -> Transcript:
    return Transcript {
        hash: 4
    }

def append_message_u32(Transcript t, u32 f) -> Transcript:
    // t.hash = t.hash + (F)f
    return t

def append_message_field(Transcript t, field f) -> Transcript:
    // t.hash = t.hash + f
    return t